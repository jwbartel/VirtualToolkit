package bus.uigen.widgets.swing;import javax.swing.JTextArea;import bus.uigen.widgets.TextAreaFactory;import bus.uigen.widgets.VirtualTextArea;public class SwingTextAreaFactory implements TextAreaFactory {	static int id;	public VirtualTextArea createTextArea(String text) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createJTextArea(text);	}	public VirtualTextArea createTextArea() {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createJTextArea();	}	public VirtualTextArea createTextArea(String text, int rows, int cols) {		return createJTextArea(text, rows, cols);	}	static int getNewID() {		return id++;	}	public static VirtualTextArea createJTextArea(String text) {		JTextArea textArea = new JTextArea(text);		VirtualTextArea toReturn = SwingTextArea.virtualTextArea(textArea);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}	public static VirtualTextArea createJTextArea(String text, int rows,			int cols) {		JTextArea textArea = new JTextArea(text, rows, cols);		VirtualTextArea toReturn = SwingTextArea.virtualTextArea(textArea);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}	public static VirtualTextArea createJTextArea() {		JTextArea textArea = new JTextArea();		VirtualTextArea toReturn = SwingTextArea.virtualTextArea(textArea);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}}