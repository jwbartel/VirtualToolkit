package bus.uigen.widgets.swt;import java.util.Hashtable;import org.eclipse.swt.widgets.Menu;import bus.uigen.widgets.MenuBarFactory;import bus.uigen.widgets.VirtualMenuBar;public class SWTMenuBarFactory implements MenuBarFactory {	static int id;	public VirtualMenuBar createMenuBar(String text) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createSWTMenuBar(text);	}	/*	 * public VirtualMenuBar createMenuBar (Object icon) {	 * 	 * return createMenuBar((Icon) icon); }	 */	public VirtualMenuBar createMenuBar() {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createSWTMenuBar();	}	static int getNewID() {		return id++;	}	public static SWTMenuBar createSWTMenuBar(String text) {		// MenuBar menuItem = new MenuBar();		return new SWTMenuBar(text);		// return virtualMenuBar(menuItem);		// return new Panel();		// return new JPanel();	}	/*	 * public static AnAWTMenuBar createAWTMenuBar (Icon icon) { MenuBar	 * menuItem = new MenuBar(icon); return virtualMenuBar(menuItem); //return	 * new Panel(); //return new JPanel(); }	 */	public static SWTMenuBar createSWTMenuBar() {		// MenuBar menuItem = new MenuBar();		// Menu menuBar = new Menu()		return new SWTMenuBar();		// return virtualMenuBar(menuItem);		// return new Panel();		// return new JPanel();	}	static transient Hashtable<Menu, SWTMenuBar> MenuBarsToVirtualMenuBars = new Hashtable<Menu, SWTMenuBar>();	public static SWTMenuBar virtualMenuBar(Menu theMenuBar) {		if (theMenuBar == null)			return null;		SWTMenuBar vc = MenuBarsToVirtualMenuBars.get(theMenuBar);		if (vc == null) {			vc = new SWTMenuBar(theMenuBar);			MenuBarsToVirtualMenuBars.put(theMenuBar, vc);		}		return vc;	}}