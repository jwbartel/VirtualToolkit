package bus.uigen.widgets.swt;

import bus.uigen.widgets.CheckBoxFactory;
import bus.uigen.widgets.VirtualCheckBox;

public class SWTCheckBoxFactory implements CheckBoxFactory {

	@Override
	public VirtualCheckBox createCheckBox() {
		/*
		 * Button checkBox = new Button(shell, SWT.CHECK); SWTCheckBox toReturn
		 * = new SWTCheckBox(checkBox); toReturn.init(); return toReturn;
		 */
		return null;
	}
}
