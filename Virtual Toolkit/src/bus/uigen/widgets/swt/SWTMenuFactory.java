package bus.uigen.widgets.swt;import java.util.Hashtable;import org.eclipse.swt.widgets.Menu;import bus.uigen.widgets.MenuFactory;import bus.uigen.widgets.VirtualMenu;public class SWTMenuFactory implements MenuFactory {	static int id;	public VirtualMenu createMenu(String text) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createSWTMenu(text);	}	/*	 * public VirtualMenu createMenu (Object icon) {	 * 	 * return createMenu((Icon) icon); }	 */	public VirtualMenu createMenu() {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createSWTMenu();	}	static int getNewID() {		return id++;	}	public static SWTMenu createSWTMenu(String text) {		// Menu menuItem = new Menu(text);		// return virtualMenu(menuItem);		return new SWTMenu(text);		// return new Panel();		// return new JPanel();	}	/*	 * public static AnAWTMenu createAWTMenu (Icon icon) { Menu menuItem = new	 * Menu(icon); return virtualMenu(menuItem); //return new Panel(); //return	 * new JPanel(); }	 */	public static SWTMenu createSWTMenu() {		// Menu menuItem = new Menu();		// return virtualMenu(menuItem);		return new SWTMenu();		// return new Panel();		// return new JPanel();	}	static transient Hashtable<Menu, SWTMenu> MenusToVirtualMenus = new Hashtable<Menu, SWTMenu>();	public static SWTMenu virtualMenu(Menu theMenu) {		if (theMenu == null)			return null;		SWTMenu vc = MenusToVirtualMenus.get(theMenu);		if (vc == null) {			vc = new SWTMenu(theMenu);			MenusToVirtualMenus.put(theMenu, vc);		}		return vc;	}}