package bus.uigen.widgets.swing;import javax.swing.Icon;import javax.swing.JLabel;import bus.uigen.widgets.LabelFactory;import bus.uigen.widgets.VirtualLabel;public class SwingLabelFactory implements LabelFactory {	static int id;	public VirtualLabel createLabel() {		return new SwingLabel();	}		protected JLabel createJLabel(String aText) {		return new JLabel(aText);	}	public VirtualLabel createLabel(String text) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createLabelStatic(createJLabel(text));	}	public VirtualLabel createLabel(Object icon) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createLabelStatic((Icon) icon);	}	static int getNewID() {		return id++;	}	public static VirtualLabel createLabelStatic(String text) {		return createLabelStatic (new JLabel(text));//		JLabel label = new JLabel(text);//		VirtualLabel toReturn = SwingLabel.virtualLabel(label);//		return toReturn;			}	public static VirtualLabel createLabelStatic(JLabel label) {		VirtualLabel toReturn = SwingLabel.virtualLabel(label);		return toReturn;			}	public static VirtualLabel createLabelStatic(Icon icon) {		JLabel label = new JLabel(icon);		VirtualLabel toReturn = SwingLabel.virtualLabel(label);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}}