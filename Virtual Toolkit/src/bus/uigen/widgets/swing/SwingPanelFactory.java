package bus.uigen.widgets.swing;import javax.swing.JPanel;import bus.uigen.widgets.ContainerFactory;import bus.uigen.widgets.PanelFactory;import bus.uigen.widgets.VirtualContainer;public class SwingPanelFactory implements PanelFactory, ContainerFactory {	static int id;	public VirtualContainer createPanel() {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createPanelStatic(createJPanel());	}		protected JPanel createJPanel() {		return new JPanel();	}			static int getNewID() {		return id++;	}	public static VirtualContainer createPanelStatic(JPanel panel) {//		JPanel panel = new JPanel();				panel.setName("" + getNewID());		// panel.setBackground(Color.white);		VirtualContainer toReturn = SwingPanel.virtualContainer(panel);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}	// public static JPanel createJPanel () {	public static VirtualContainer createPanelStatic() {		JPanel panel = new JPanel();		return createPanelStatic(panel);//		//		panel.setName("" + getNewID());//		VirtualContainer toReturn = SwingPanel.virtualContainer(panel);//		return toReturn;		}	public VirtualContainer createContainer() {		return createPanelStatic();	}}