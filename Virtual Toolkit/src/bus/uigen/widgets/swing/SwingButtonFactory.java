package bus.uigen.widgets.swing;import javax.swing.Icon;import javax.swing.JButton;import bus.uigen.widgets.ButtonFactory;import bus.uigen.widgets.VirtualButton;public class SwingButtonFactory implements ButtonFactory {	static int id;	public VirtualButton createButton(String text) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createJButton(text);	}	public VirtualButton createButton(Object icon) {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createJButton((Icon) icon);	}	public VirtualButton createButton() {		/*		 * Container panel = new JPanel(); panel.setName("" + getNewID());		 * //panel.setBackground(Color.white); return panel; //return new		 * Panel(); //return new JPanel();		 */		return createJButton();	}	static int getNewID() {		return id++;	}	public static SwingButton createJButton(String text) {		JButton button = new JButton(text);		SwingButton toReturn = SwingButton.virtualButton(button);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}	public static SwingButton createJButton(Icon icon) {		JButton button = new JButton(icon);		SwingButton toReturn = SwingButton.virtualButton(button);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}	public static SwingButton createJButton() {		JButton button = new JButton();		SwingButton toReturn = SwingButton.virtualButton(button);//		toReturn.init();		return toReturn;		// return new Panel();		// return new JPanel();	}}